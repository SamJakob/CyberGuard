import 'package:flutter/material.dart';

class LevelScoreWheel extends StatelessWidget {
  const LevelScoreWheel({final Key? key}) : super(key: key);

  @override
  Widget build(final BuildContext context) {
    return const Placeholder();
  }
}
